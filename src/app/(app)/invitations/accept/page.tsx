import {AcceptInvitation}from"@/components/settings/accept-invitation";import {Card,CardContent,CardHeader,CardTitle}from"@/components/ui/card";
export const metadata={title:"Accetta invito"};export default async function AcceptInvitationPage({searchParams}:{searchParams:Promise<{token?:string}>}){const{token}=await searchParams;return <Card className="mx-auto max-w-lg"><CardHeader><CardTitle>Invito a collaborare</CardTitle></CardHeader><CardContent className="space-y-3"><p className="text-sm text-muted-foreground">Accetta per entrare nel workspace con il ruolo assegnato. L’indirizzo del tuo account deve coincidere con quello invitato.</p>{token?<AcceptInvitation token={token}/>:<p className="text-sm text-destructive">Token mancante.</p>}</CardContent></Card>}
